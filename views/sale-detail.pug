extends layout

block content
   
    h1= product.name
    p Price: $ #{product.price}
    p Size: #{product.size}
    p
        img.image(src=product.image_path width='100px' height='100px')
    p 
        a(href='/sales/sale-edit/'+product.id) Edit Product
    p 
        a(href='/sales/sale-delete/'+product.id) Delete Product
    
    p
    
   
    
